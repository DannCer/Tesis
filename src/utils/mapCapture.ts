/**
 * @deprecated Este archivo es un duplicado de `@utils/map/mapCapture`.
 * Usar siempre el import desde la ruta canónica:
 *
 *   import { captureLeafletMap } from '@utils/map/mapCapture';
 *
 * Este archivo se mantiene temporalmente para evitar romper posibles
 * imports externos, pero será eliminado en la próxima limpieza.
 */
export * from './map/mapCapture';
